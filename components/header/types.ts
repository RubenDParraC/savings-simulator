/**
 * ButtonListItem
 *
 * Defines the structure of a navigation button used in the Header.
 */
export interface ButtonListItem {
  /**
   * Route path used for navigation.
   */
  href: string;

  /**
   * Visible label displayed to the user.
   */
  label: string;
}
